'use strict';

describe('myApp.pictures module', function() {

  beforeEach(module('myApp.pictures'));

  describe('pictures controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var picturesCtrl = $controller('PicturesCtrl');
      expect(picturesCtrl).toBeDefined();
    }));

  });
});